import React from 'react';

import Layout from '../components/layout'

const portfolioPage = () => (
    <Layout>
        <div style={{display: 'flex', height: '100vh', fontSize: '4rem', alignItems: 'center', justifyContent: 'center'}}>Portfolio coming soon.</div>
    </Layout>
)

export default portfolioPage;