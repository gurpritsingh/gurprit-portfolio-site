import React from 'react';

import Layout from '../components/layout'

const AboutPage = () => (
    <Layout>
        <div style={{display: 'flex', height: '100vh', fontSize: '4rem', alignItems: 'center', justifyContent: 'center'}}>About me coming soon.</div>
    </Layout>
)

export default AboutPage;